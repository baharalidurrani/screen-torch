{
  "name": "screen-torch-pwa",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "clean": "rm -rf dist/",
    "build": "tsc && vite build",
    "preview": "vite preview --host",
    "serve": "yarn clean && yarn build && yarn preview",
    "dep:update": "npx npm-check-updates -u"
  },
  "devDependencies": {
    "typescript": "^5.4.3",
    "vite": "^5.2.3",
    "vite-plugin-mkcert": "^1.17.5",
    "vite-plugin-pwa": "^0.19.7"
  }
}
